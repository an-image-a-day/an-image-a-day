#!/bin/bash

if ! [[ `git remote -v | awk '{print $1}' | grep heroku` =~ ^heroku$ ]]; then
  git remote add heroku git@heroku.com:an-image-a-day.git
fi

CURRENT_BRANCH=`git branch | grep \\* | cut -d ' ' -f2`
git subtree push --prefix aiad-web heroku $CURRENT_BRANCH
